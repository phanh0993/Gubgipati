/**
 * Supabase Client - Re-export từ config tập trung
 * File này giữ lại để tương thích với code cũ
 * Nên sử dụng import từ '../config/supabase' trong code mới
 */

// Re-export từ config tập trung
export { supabase, supabaseConfig } from '../config/supabase';
export { default } from '../config/supabase';


